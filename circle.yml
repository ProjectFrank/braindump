test:
  post:
    - python manage.py test --coverage
    - cp -R tmp/coverage/* $CIRCLE_ARTIFACTS
    - mkdir -p $CIRCLE_TEST_REPORTS/python/
    - mv *.xml $CIRCLE_TEST_REPORTS/python/
    - echo "dsafkdjsafldajsfladjf" > $CIRCLE_TEST_REPORTS/python/invalid.xml
